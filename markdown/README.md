### MD to html converter

1) `pip install markdown`
2) `python markdown/main.py MetaC-vault/Standard -o converted/`
where MetaC-vault/Standard folder with docs and converted is output directory.
You need finally copy converted/ and theme.css files to html web server folder.