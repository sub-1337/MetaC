Мы можем получить всю иерархию классов.

По умолчанию для множественного наследования применяется
виртуальное наследование.
```
class Base
{}
class A(Base)
{}
class B(Base)
{}
class Derived(A,B) // Будет иметь 1 предка Base
{}
```

```
class Base
{}
class A(copy Base)
{}
class B(copy Base)
{}
class Derived(A,B) // Будет иметь 2 предка Base
{}

let der : Derived = new Derived();
let abase : type(der)::A::Base = der; // Апкаст к 1 из 2х родителейы
```
