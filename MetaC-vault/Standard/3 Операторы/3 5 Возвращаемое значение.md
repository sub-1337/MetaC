Как в rust в нашем языке если выражение состоит из одной переменной
мы считаем её возвращаемым типом.
Мы можем написать слово return явно если хотим.

```
let a = 10;
let b = {10};
let c = {return 10;}
```
Все переменные равны 10.